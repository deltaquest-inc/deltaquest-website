{
  /*import { allBlogs } from '../contentlayer/generated'

export function getLocalizedBlogs(locale: 'fr' | 'en' | 'ja') {
  return allBlogs.filter((blog) => blog.locale === (locale as string))
}*/
}
